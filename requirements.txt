flask==3.1.3
gunicorn==26.0.0
# Bumped weekly by .github/workflows/update-yt-dlp.yml — site extractors break
# constantly, so this pin is meant to move often. Keep it pinned anyway so a
# rebuild is reproducible and a bad release can be rolled back by one commit.
yt-dlp==2026.7.4
